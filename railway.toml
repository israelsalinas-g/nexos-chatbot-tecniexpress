[deploy]
startCommand = "python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT} --log-level debug"

healthcheckPath = "/health"
healthcheckTimeout = 60
healthcheckInterval = 10
restartPolicyType = "on_failure"



